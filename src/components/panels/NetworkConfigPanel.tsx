import { PageHeader } from '../ui/PageHeader';
export function NetworkConfigPanel() {
  return <div id="panel-network-config" className="panel"><PageHeader title="Configure Network" subtitle="Manage routes, DNS, and access" /></div>;
}
